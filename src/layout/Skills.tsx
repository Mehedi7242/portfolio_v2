
import SkillSlider from './SkillSlider';

const Skills = () => {
    return (
        <section id='skills' className="scroll-mt-14 py-12 text-center">
            <h2 className="text-3xl font-bold mb-6 text-gray-800 dark:text-gray-200">
                My Tech Stack
            </h2>
            <SkillSlider />
        </section>

    );
};

export default Skills;